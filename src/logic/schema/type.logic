// Types
type(Type), type:id(Type:Ref) -> string(Ref).


// Sizes of types in bytes

type:size[Type] = Size ->
   type(Type), int[64](Size).

type:unpadded_size[Type] = Size ->
   type(Type), int[64](Size).
