// Core concepts

constant_points_to(Alloc, Constant) ->
   allocation(Alloc), constant(Constant).

var_points_to(Alloc, Var) ->
   allocation(Alloc), variable(Var).

ptr_points_to(Alloc, PtrAlloc) ->
   allocation(Alloc), allocation(PtrAlloc).
